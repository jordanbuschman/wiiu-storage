<% layout('boilerplate') -%>
<% block('title', title) %>

<% if (message) { %>
    <p id="message"><%= message %></p>
<% } %>

<% if (!loggedIn) { %>
    <% include login-modal %>
<% } %>

<h4>Wii-U File Storage</h4>
<p>View, upload, and download files, or log in to view private files.</p>
